import pandas as pd
import numpy as np

# Go into each sheet and extract the data (companies, weights and emissions)

# Go into values sheet and extract values



def extract_sheet_data(file):
    # Get list of sheet names.
    get_date_list(file)